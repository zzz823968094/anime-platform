server:
  port: 8084

spring:
  application:
    name: danmaku-service
  cloud:
    nacos:
      discovery:
        server-addr: 10.15.0.5:8848
        username: nacos
        password: nacos
      config:
        server-addr: 10.15.0.5:8848
        username: nacos
        password: nacos
        file-extension: yml
        shared-configs:
          - data-id: mysql-redis.yml
            refresh: true
