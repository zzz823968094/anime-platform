server:
  port: 8084

spring:
  application:
    name: danmaku-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        shared-configs:
          - data-id: mysql-redis.yml
            refresh: true
